NODE_ENV=development
PORT=5000
=
DATABASE_URL="postgres://USER:PASSWORD@HOST:5432/DBNAME?sslmode=require"
=
# Optional, migrations only. Set this ONLY if DATABASE_URL carries
# `channel_binding=require` (Neon does by default): Prisma's migration engine
# cannot negotiate it and reports `P1001: Can't reach database server` even
# though the app itself connects fine. Copy DATABASE_URL here and drop the
# `channel_binding` parameter, keeping `sslmode=require`. Leave unset
# otherwise — prisma.config.ts falls back to DATABASE_URL.
DIRECT_DATABASE_URL="postgres://USER:PASSWORD@HOST:5432/DBNAME?sslmode=require"
=
BETTER_AUTH_SECRET=your-better-auth-secret
=
BETTER_AUTH_URL=http://localhost:5000
=
ACCESS_TOKEN_SECRET=your-access-token-secret
REFRESH_TOKEN_SECRET=your-refresh-token-secret
ACCESS_TOKEN_EXPIRES_IN=1d
REFRESH_TOKEN_EXPIRES_IN=7d
=
# email Configuration
EMAIL_SENDER_SMTP_USER=your-smtp-user@example.com
EMAIL_SENDER_SMTP_PASS=your-smtp-app-password
EMAIL_SENDER_SMTP_HOST=smtp.gmail.com
EMAIL_SENDER_SMTP_PORT=465
EMAIL_SENDER_SMTP_FROM=your-from-address@example.com
=
=
# Google OAuth Configuration
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
GOOGLE_CALLBACK_URL=
=
=
FRONTEND_URL=http://localhost:3000
=
# Origin of the deployed admin panel. Optional locally (the allowlist already
# carries the localhost dev ports), REQUIRED once the panel is deployed.
#
# A third origin, distinct from FRONTEND_URL (the storefront) and
# BETTER_AUTH_URL (this server). Without it, every admin request fails CORS
# preflight — which looks like "the panel loads but nothing works" rather than
# like a configuration problem. It feeds both the CORS allowlist in app.ts and
# better-auth's trustedOrigins in lib/auth.ts.
#
# Origin only — no trailing slash, no path.
ADMIN_URL=
=
CLOUDINARY_CLOUD_NAME=your-cloud-name
CLOUDINARY_API_KEY=your-cloudinary-api-key
CLOUDINARY_API_SECRET=your-cloudinary-api-secret
=
SUPER_ADMIN_EMAIL=
SUPER_ADMIN_PASSWORD=
SUBSCRIPTION_BKASH_NUMBER=
# Storefront cache revalidation (optional). Must match REVALIDATE_SECRET in
# frontend/.env.local. STOREFRONT_URL defaults to FRONTEND_URL when unset.
STOREFRONT_REVALIDATE_SECRET=
STOREFRONT_URL=http://localhost:4000
=
# REQUIRED. The key every integration credential in the database is encrypted
# under (AES-256-GCM). The server REFUSES TO BOOT without it — deliberately,
# because the alternative is a deployment that silently stores merchant API keys
# in readable form.
#
# Generate one:
#   node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"
#
# LOSING THIS IS NOT RECOVERABLE. Every stored credential becomes undecryptable
# and the merchant must re-enter every integration through the admin panel. Keep
# it in a password manager, not only here. Do not reuse one environment's key in
# another, and do not change it on a deployment that already has stored
# credentials — there is no rotation tooling.
INTEGRATION_ENCRYPTION_KEY=

# Steadfast Courier — FALLBACK ONLY, kept for the upgrade across
# rename-courier-setting-to-integrations. Credentials now live encrypted in the
# database and are entered by the merchant at admin UI → Integrations.
#
# These are read ONLY when no stored credential exists. On first boot whatever
# is here is imported into the encrypted table, so an existing shop keeps
# dispatching without re-entering anything. After that the table wins: env can
# fill an absent value but can never override one the merchant has set.
#
# A fresh install should leave these blank and use the admin panel.
STEADFAST_API_KEY=
STEADFAST_SECRET_KEY=
# Optional — the live API (https://portal.packzy.com/api/v1) when unset.
# Set it only to point at a sandbox account.
STEADFAST_BASE_URL=
# Shared secret for POST /courier/sync, which Vercel Cron calls on a schedule.
# Unset means the reconciliation endpoint refuses every call; the webhook and
# dispatch still work, but a missed notification is never caught up.
COURIER_SYNC_SECRET=
# Bearer token Steadfast presents when calling the webhook endpoint —
# FALLBACK ONLY, like the keys above. Generate and regenerate it from
# admin UI → Integrations, which also shows the callback URL to paste into
# Steadfast's portal under Webhook Integration → "Auth Token (Bearer)".
#
# Imported on first boot so a portal already pointing at the previous callback
# URL keeps being accepted. A webhook that silently stops arriving is invisible
# until parcels appear stuck, which is why the old URL is not retired.
#
# Register the webhook in their portal LAST, only after this endpoint is
# deployed and answering: registering against a 404 drops real notifications
# with no record they were sent. No stored token and none here means the
# endpoint refuses every call.
STEADFAST_WEBHOOK_TOKEN=
