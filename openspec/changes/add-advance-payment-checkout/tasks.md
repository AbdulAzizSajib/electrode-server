## 1. Settings shape — the contract every other group reads

- [x] 1.1 Add `advancePaymentSchema` to `server/src/app/module/store-setting/store-setting.validation.ts`: `enabled: boolean`, `mobileAccounts: array`, `bankAccounts: array`, each account with a generated `id` (slug-pattern, like `deliveryOptionSchema.key`), and `.strict()` throughout. Mobile accounts carry provider (`BKASH`/`NAGAD`/`ROCKET`), number, and an account-type label; bank accounts carry bank name, account name, account number, branch and routing number. Verify by unit-parsing a valid and an unknown-key payload and confirming the second is rejected.
- [x] 1.2 Attach it to `checkoutConfigSchema` as `advancePayment: advancePaymentSchema.optional()` — optional, per design.md Decision 1, so an existing row still parses. Verify `checkoutConfigSchema.safeParse` succeeds on a stored config captured from a current row that has no `advancePayment` key.
- [x] 1.3 Add a `superRefine` to `checkoutConfigUpdateSchema` refusing `enabled: true` with both account lists empty, and refusing duplicate account ids across both lists. Verify each rejection by unit-parsing and reading the message.
- [x] 1.4 Normalize an absent `advancePayment` to `{ enabled: false, mobileAccounts: [], bankAccounts: [] }` in `checkoutConfigOf` / the public settings read in `store-setting.service.ts`, without adding a second `withDeliveryDefault`-style shim. Verify a store row with no key reads back as disabled with empty lists, and that a row with a real `delivery` block still returns the merchant's own delivery options untouched.
- [x] 1.5 Update the `checkoutConfig` doc comment in `server/prisma/schema/StoreSetting.prisma` to document `advancePayment` **and** the already-missing `delivery` key (the drift named in proposal.md — Impact). Verify by reading the comment against `checkoutConfigSchema` field by field.
- [x] 1.6 Mirror the shape into `nextjs/src/types/store-settings.ts` (`AdvancePaymentConfig`, account types, added to `CheckoutConfig`) and into `FALLBACK_SETTINGS` as disabled-with-empty-lists. Verify `npm run build --workspace nextjs` type-checks.
- [x] 1.7 Mirror the same shape and a `DEFAULT_ADVANCE_PAYMENT` constant into `admin/src/lib/api/store-settings.ts` beside `DEFAULT_CHECKOUT_CONFIG`. Verify `npm run build --workspace admin` type-checks.

## 2. Schema — the claim and its decision

- [x] 2.1 Add the claim columns to `server/prisma/schema/Payment.prisma`, all nullable: sender identifier, merchant account id, a Json snapshot of the account details as they stood at placement, verifier user id (relation to `User`, `onDelete: SetNull`), verified-at timestamp, and rejection reason. Use `///` doc comments stating why each exists and that `transactionId`'s existing `@unique` is what refuses a reused reference. Verify `npx prisma validate`.
- [x] 2.2 Add an index supporting "find every payment awaiting verification" (status + method, or a dedicated predicate index) so the admin queue in group 6 is not a sequential scan. Verify by reading the generated migration SQL.
- [x] 2.3 Run `npm run migrate --workspace server`, then **open the generated SQL and delete the three `DROP INDEX` lines** for `Product_name_trgm_idx`, `Product_sku_trgm_idx`, `Brand_name_trgm_idx`, carrying forward the NOTE block from the most recent migration. Verify by grepping the new migration for `DROP INDEX` and finding none, and for `NOTE` and finding the block.
- [x] 2.4 Confirm no enum changes were needed — `PaymentStatus.PROCESSING`/`FAILED` and `PaymentMethod.BKASH`/`NAGAD`/`ROCKET`/`BANK_TRANSFER` already exist. Verify by grepping `enums.prisma` for each value.

## 3. Order placement — accepting a claim

- [x] 3.1 Widen `paymentMethod: z.literal("COD")` in `server/src/app/module/order/order.validation.ts` to the checkout-reachable methods, and add the advance-payment sub-payload (choice, merchant account id, sender identifier, transaction id) as a discriminated shape so a non-advance order cannot carry half a claim. Replace the `z.literal` comment, which explains a constraint that no longer holds. Verify by unit-parsing: a plain COD order, a complete claim, and a claim missing its transaction id (rejected).
- [x] 3.2 Add the advance-amount computation to `server/src/app/module/order/order.pricing.ts` (or beside the existing total computation): delivery-charge-only → the resolved delivery option's price; full-payment → the order total. Return both the advance and the remaining balance. Verify the two sum to the order total for both choices across a fixture set including a free-shipping-threshold order.
- [x] 3.3 Surface the advance amount from the checkout quote endpoint so the storefront can state it before asking for a transaction id. Verify `GET`/quote response carries advance and balance for each choice.
- [x] 3.4 In `order.service.ts`, replace the non-COD rejection at the guest branch (`~:769`) with a check that the submitted method is permitted by the store's current `advancePayment` config. Verify: with the feature off, a bKash order is refused naming the store setting; with it on, it is accepted.
- [x] 3.5 Validate the claim before creating anything: the merchant account id must exist in the store's configured accounts, and the transaction id must not already be recorded — throw 409 naming the reused reference rather than letting `P2002` surface as a 500. Verify both rejections, and verify no `Order` row is created by either.
- [x] 3.6 Make the `payments: { create: ... }` block (`~:1266-1296`) conditional: an advance order creates one `PROCESSING` row of the computed advance amount with the claim fields and account snapshot; a non-advance order creates the existing `COD`/`PENDING` row for the full total, unchanged. Update the surrounding comment, which currently states that advance money is recorded afterwards — design.md Decision 3 explains why this is a narrow exception. Verify both shapes by placing one order of each and reading the payment rows.
- [x] 3.7 Revalidate the client-supplied advance amount against the server's own computation at placement and refuse a mismatch with 409, alongside the existing `expectedTotal` check. Verify by posting a stale amount after a delivery-option change.
- [x] 3.8 Confirm stock and price validation still run **before** the claim is accepted, so the common failure happens before the shopper is asked to send money (design.md — Risks). Verify by placing an out-of-stock advance order and confirming the rejection cites stock.

## 4. The blocking rule

- [x] 4.1 Add the predicate — does this order have a `Payment` in `PROCESSING` with an advance method — as a single named helper in `order.service.ts`, used by every caller rather than reimplemented. Verify by unit-calling it against an order with a verified claim, an unverified claim, a rejected claim, and no claim at all.
- [x] 4.2 Assert it in `updateOrderStatus` **before** the no-op and `assertOrderTransitionAllowed` checks (design.md Decision 5 — ordering decides which error the admin reads), throwing 409 naming verification. Permit `CANCELLED` through. Verify: confirming an unverified order is refused citing verification; cancelling it succeeds and restocks; a legal transition on a verified order succeeds unchanged.
- [x] 4.3 Confirm the customer self-cancel path (`~:2077`) is unaffected — cancel stays permitted for an unverified order. Verify by self-cancelling one.
- [x] 4.4 Confirm courier dispatch needs no change: it refuses anything not `PACKED`, which an unverified order cannot reach. Verify by attempting dispatch on an unverified order and reading the refusal.
- [x] 4.5 Add `npx tsx scripts/verify-advance-payment-blocking.ts` covering 4.2–4.4 end to end on `__verify_*`-prefixed rows with cleanup in a `finally`. Verify the script passes.

## 5. Verification operations

- [x] 5.1 Add `verifyAdvancePayment` and `rejectAdvancePayment` to `server/src/app/module/payment/payment.service.ts`, each taking `userId` first, writing verifier and timestamp, reusing `updatePaymentStatus`'s sold-count and `paidAt` internals rather than reimplementing them. Reject requires a reason (400 without one). Both refuse an already-decided payment with 409. Verify each path plus the already-decided case.
- [x] 5.2 Record an `AuditLogService.record(...)` on each decision naming actor, order, payment, decision and amount, so a later verification over an earlier rejection leaves both in the trail. Verify by verifying a previously rejected payment and reading back two audit entries.
- [x] 5.3 Add the routes to `payment.route.ts` gated to `ADMIN_PANEL_ROLES`, and add validation schemas in `payment.validation.ts`. Verify a customer session receives 403 on both.
- [x] 5.4 Close the self-release hole: constrain `POST /orders/:id/payments` so a non-staff caller cannot set `status` (design.md Decision 6). Verify a customer cannot create a `PAID` row on their own order.
- [x] 5.5 Add an endpoint or query filter listing payments awaiting verification, so the admin queue is not per-order. Verify it returns only `PROCESSING` advance claims and uses the index from 2.2.
- [x] 5.6 Add `npx tsx scripts/verify-advance-payment-verification.ts` pinning: staff-only access, reason-required rejection, no double decision, the audit receipt on each decision, and that a customer cannot self-verify by any route. Verify the script passes.

## 6. Admin

- [ ] 6.1 Add a payment-accounts section to `admin/src/features/ui/checkout-settings/checkout-settings-page.tsx` — an enable toggle plus add/remove/reorder lists for mobile-banking and bank accounts, generating a stable account id on creation and never rewriting it on edit (design.md Decision 2). Follow the existing `useSettingsDraft` / `EditorSection` / `EditorActions` pattern. Verify the page saves and reloads with the accounts intact.
- [ ] 6.2 Keep the page's partial-PATCH discipline: it must still send only `checkoutConfig`, with `advancePayment` inside it, and must not be able to express a configuration the backend refuses — disable the enable toggle while both lists are empty rather than letting the save 400. Verify by saving from this page and confirming no other settings block changed.
- [ ] 6.3 Add the claim panel to the order detail page showing amount claimed, method, merchant account, sender identifier, transaction reference, the decision with actor and time, and any rejection reason — alongside the existing balance-due line, which needs no change. Verify against an unverified, a verified and a rejected order.
- [ ] 6.4 Add Verify and Reject actions (reject prompting for a reason) wired to the 5.3 endpoints, hidden for non-staff roles and for already-decided claims. Verify the order becomes confirmable immediately after verifying, without a reload.
- [ ] 6.5 Surface the awaiting-verification queue from 5.5 in the admin. If it needs its own route, register it in **both** `src/routes/nav-config.ts` and `src/routes/app-router.tsx` with a matching `<RoleGuard>`. Verify the route renders and the sidebar entry appears for the intended roles only. (design.md — Open Questions leaves page-vs-filter open; either satisfies this task.)
- [ ] 6.6 Correct the stale header comment in `admin/src/lib/api/payments.ts` claiming "no update/void endpoint exists". Verify by reading it against `payment.route.ts`.

## 7. Storefront

- [ ] 7.1 Replace the static COD panel in `nextjs/src/components/checkout/CheckoutForm.tsx` (`~:879-895`) with the payment choice section: rendered only when the store has advance payment on, offering the two choices, defaulting to neither. Verify the panel is byte-for-byte the current COD copy when the feature is off.
- [ ] 7.2 Build the claim form matching the reference design: a mobile-banking / bank-transfer tab switch, the merchant's configured accounts as selectable cards, the selected account's details with copy buttons, the amount to send stated prominently, the remaining balance stated as its own line, then sender identifier and transaction id inputs — both required. Verify against the reference screenshots at mobile width.
- [ ] 7.3 Send the claim in the place-order payload alongside the existing hardcoded `paymentMethod` (`~:475`), which becomes the selected method. Verify a placed advance order arrives with all four claim values.
- [ ] 7.4 Handle the new failures with their own copy, not the generic message: 409 duplicate transaction id, 409 stale advance amount after a delivery-option change, and 400 unconfigured account. Verify each by forcing the condition.
- [ ] 7.5 Recompute the stated advance when the delivery option changes, so the shopper is never shown a stale figure (spec: "Delivery option changed after a choice is made"). Verify by switching options with a choice already selected.
- [ ] 7.6 Confirm the order-placed confirmation tells the shopper their payment is awaiting verification rather than implying the order is confirmed. Verify by reading the success page after an advance order.

## 8. Cross-cutting verification

- [x] 8.1 Add `npx tsx scripts/verify-advance-payment-settings.ts` asserting the three hand-synced shapes agree — Zod, `nextjs/src/types/store-settings.ts`, `admin/src/lib/api/store-settings.ts` — the same obligation `verify-revalidate-tags.ts` discharges for tags. Verify the script passes, and that it fails when a field is renamed in one place only.
- [x] 8.2 Assert the feature-off path is unchanged: a store with no `advancePayment` key places a plain COD order through the identical code path, and the blocking predicate is never consulted for an order with no advance payment row (design.md — Risks, "Feature off must be provably unchanged"). Verify by script.
- [ ] 8.3 Run the full verify suite plus `npm run lint --workspace server`, `npm run test --workspace admin` and `npm run test --workspace nextjs`. Verify all pass.
- [ ] 8.4 Check whether any storefront-cached resource is affected by the new settings key; `store-settings` is an existing tag, so confirm the settings mutation already fires it and no new tag is needed. Verify by editing accounts in the admin and observing the storefront pick them up without waiting out a revalidate window.
