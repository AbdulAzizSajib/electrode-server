## 1. Server — register the section's layouts

- [x] 1.1 In `server/src/app/module/store-setting/store-setting.constant.ts`, add `FEATURED_CATEGORIES_VARIANTS = ["GRID", "SLIDER"] as const` beside `HERO_VARIANTS`, with `GRID` at position 0 and the same order-is-load-bearing warning `HERO_VARIANTS` carries; add `FEATURED_CATEGORIES: FEATURED_CATEGORIES_VARIANTS` to `HOME_SECTION_VARIANTS`. Update the doc comment on `HOME_SECTION_VARIANTS` and on `HomeSectionConfig.variant`, which says the choice is "today `HERO` alone". Verify `npx tsc --noEmit` in `server/` is clean and nothing else references a hero-only assumption (`grep -rn "HERO alone\|HERO_VARIANTS" server/src`).
- [x] 1.2 Rename `scripts/verify-hero-section-variants.ts` to `scripts/verify-section-variants.ts` and generalise it to iterate every key of `HOME_SECTION_VARIANTS`: for each section, every offered layout round-trips validation and reconciliation, absent resolves to position 0, an unknown value is rejected with a message naming the offered layouts, a layout on a section that offers none is rejected, and resolution never rewrites the stored row. Update the reference in `server/CLAUDE.md` / `AGENTS.md` if either names the old script. Verify `npx tsx scripts/verify-section-variants.ts` passes and its output lists both `HERO` and `FEATURED_CATEGORIES`.
- [x] 1.3 Confirm no route, controller, service, validation or Prisma change is needed: `homeConfigSchema` and `reconcileHomeConfig` are generic over the map. Verify by writing `{ key: "FEATURED_CATEGORIES", enabled: true, variant: "SLIDER" }` through `homeConfigSchema.safeParse` in the verify script (task 1.2 covers it) and by `PATCH /settings` with that entry against a running server returning 200, then `GET /settings/public` reporting `SLIDER` on the section. **Done via the service, not HTTP:** `StoreSettingService.updateStoreSetting` with the parsed entry (the repo's verify-script approach — an HTTP `PATCH` needs an admin's two login cookies), then `GET /settings/public` reported `SLIDER` on the section. The route→controller path is a pass-through the hero already exercised.

## 2. Storefront — types, resolver, fallback

- [x] 2.1 In `nextjs/src/types/store-settings.ts`, add `FeaturedCategoriesLayout = "GRID" | "SLIDER"` with the hand-maintained-mirror warning the `HeroVariant` comment carries, and widen `HomeSection.variant` to `HeroVariant | FeaturedCategoriesLayout`. Rewrite that field's comment, which says the choice is "today `HERO` alone". Verify `npx tsc --noEmit` in `nextjs/` is clean.
- [x] 2.2 Replace `nextjs/src/lib/hero-variants.ts` with `lib/section-layouts.ts`: a `SECTION_LAYOUTS` map mirroring the server's `HOME_SECTION_VARIANTS` (`HERO` and `FEATURED_CATEGORIES`, each an ordered tuple, position 0 the default), `defaultLayout(key)`, and `resolveSectionLayout(key, value)` with the hero resolver's exact rule. Delete `resolveHeroVariant` and update its two call sites (`app/(shop)/page.tsx`, and `hero/registry.ts` if it imports the keys). Verify `npx tsc --noEmit` is clean and `grep -rn "resolveHeroVariant\|hero-variants" nextjs/src` returns nothing outside the renamed test.
- [x] 2.3 Rename `lib/hero-variants.test.ts` to `lib/section-layouts.test.ts` and generalise: for both sections, a valid value round-trips, absent/unknown/non-string resolve to position 0, position 0 is `SPLIT_THREE` for `HERO` and `GRID` for `FEATURED_CATEGORIES`, and the withdrawn `SPLIT_ONE` case still resolves to the hero default. Verify `npm run test --workspace nextjs -- src/lib/section-layouts.test.ts` passes.
- [x] 2.4 In `nextjs/src/services/store-settings.ts`, add `variant: "GRID"` to the `FEATURED_CATEGORIES` entry of `FALLBACK_SETTINGS.homeConfig`, extending the comment on the hero entry ("the one place this storefront defaults a layout itself") to cover both. Verify the existing settings-fallback tests pass (`npm run test --workspace nextjs -- src/services`).

## 3. Storefront — the section and its two layouts

- [x] 3.1 Extract the grid's tile markup from `components/home/CategoryGrid.tsx` into `components/home/categories/CategoryTile.tsx`, carried over verbatim (the `Link`, the `Image` at 500×400 `object-contain`, the `lg:h-28 lg:w-28` box, the name). Verify by diffing the rendered grid HTML before and after against a running storefront: `curl -s localhost:4000/ | grep -o '<section class="container-px site-container pb-8[^§]*Featured Categories.*</section>'` (or an equivalent extraction) is byte-identical. **Verified: 13,926 bytes before and after, identical, with the layout stored as `GRID` and with no layout stored at all.** **Then changed once, on request, after a phone screenshot:** the tile now has a fixed image band (`h-20`, `lg:h-28`) and a two-line name block (`min-h-10`, `line-clamp-2`) so every tile is the same height whatever its picture or name — applied to both layouts through the shared tile, so the grid's phone rendering now differs from the pre-change capture by exactly that. The section, heading and grid markup are still the original bytes; the spec's "identical" clause was amended to say so.
- [x] 3.2 Create `components/home/categories/CategoryGridLayout.tsx` (the existing grid markup over `CategoryTile`) and `components/home/categories/CategorySlider.tsx`, a `"use client"` component on `swiper/react` with the `Navigation` module, `slidesPerView` 3 / 4 / 7 at the grid's `sm` (640) and `lg` (1024) breakpoints, `spaceBetween: 16`, and no `Autoplay`, `loop` or `Pagination` — with a comment naming the grid class string (`grid-cols-3 gap-4 sm:grid-cols-4 lg:grid-cols-7`) these three numbers must match, and why there is no autoplay (design.md Decision 4). Verify `npx tsc --noEmit` and `npm run lint --workspace nextjs` are clean.
- [ ] 3.3 Add `CategorySliderSkeleton` to `components/home/HomeSkeletons.tsx`: one row of seven tiles at the grid skeleton's tile shape inside `overflow-hidden`, sharing the heading block. Verify it renders at the same height as one row of `CategoryGridSkeleton` by comparing the two in a browser at `lg`. **Code done** — same tile block, widths mirror `slidesPerView` at each breakpoint; the browser height comparison still needs a human.
- [x] 3.4 Create `components/home/categories/registry.ts`: `Record<FeaturedCategoriesLayout, { Component, Skeleton }>` for `GRID` and `SLIDER`, with the doc comment from `hero/registry.ts` on why the skeleton lives beside its layout. Verify `npx tsc --noEmit` reports an error if either key is removed (temporarily delete one, observe the error, restore).
- [x] 3.5 Replace `components/home/CategoryGrid.tsx` with `components/home/FeaturedCategories.tsx` on the `Hero` pattern: fetch via `getCategoryGrid()`, return `null` when empty (heading included), then render the registry's `Component` for the given layout with the list as props. Verify with the server on `SLIDER`: `curl -s localhost:4000/` contains the Swiper wrapper markup inside the Featured Categories section and none of the grid's `grid-cols-7`; on `GRID`, the reverse.
- [ ] 3.6 In `app/(shop)/page.tsx`, build `FEATURED_CATEGORIES` from its config entry as `HERO` is built: resolve the layout with `resolveSectionLayout("FEATURED_CATEGORIES", …)`, take the `Skeleton` from the registry for the Suspense fallback, and pass the layout to `FeaturedCategories`. Extend the comment block that explains why the hero is built from its entry to cover both. Verify `npx tsc --noEmit` is clean and, in a browser with throttled network, the slider's placeholder occupies one row and is replaced without the page moving. **Code done, tsc clean**; the placeholder is the registry's own `Skeleton` for the resolved layout by construction. The throttled-network check still needs a human.
- [ ] 3.7 Confirm the slider does not move on its own: load the storefront on `SLIDER`, wait thirty seconds, and verify the first visible tile is unchanged; then verify the navigation control moves the row and that the row stops at the last category rather than wrapping. **By construction:** `CategorySlider` imports only the `Navigation` module — no `Autoplay`, no `loop` — and the served HTML on `SLIDER` carries both arrow buttons. The thirty-second watch and the end-of-row check still need a human.

## 4. Admin — types, options, registry description

- [x] 4.1 In `admin/src/lib/api/store-settings.ts`, add `FeaturedCategoriesLayout = 'GRID' | 'SLIDER'` mirroring the server tuple in order, widen `HomeSection.variant` to `HeroVariant | FeaturedCategoriesLayout`, and add `FEATURED_CATEGORIES_LAYOUT_OPTIONS` beside `HERO_VARIANT_OPTIONS` with merchant-facing labels ("Grid", "Slider") and one-line descriptions. Rewrite `HOME_SECTION_REGISTRY`'s `FEATURED_CATEGORIES` description — "The grid of category tiles customers browse from" names one arrangement as if it were the only one. Verify `npx tsc --noEmit` in `admin/` is clean.

## 5. Admin — the shared picker shell and the row panel

- [x] 5.1 Extract the card shell from `features/ui/home-slider/variant-picker.tsx` into `features/ui/components/layout-picker.tsx`, generic over the option key: the Radix `RadioGroup` root and items, selected and pending states, the no-`h-full` rule and the not-shared-`isPending` rule with their comments moved intact, and `diagram`, `label`, `description`, optional `footer` per option plus an optional `columns` for the xl grid. Re-implement `VariantPicker` over it, keeping its diagrams, `BOX`, `Slot` and size-list footer where they are. Verify `npm run test --workspace admin -- src/features/ui/home-slider/variant-picker.test.tsx` passes **unchanged** — the refactor is behaviour-preserving by construction — and `npx tsc --noEmit` is clean.
- [x] 5.2 Add `features/ui/home-sections/category-layout-picker.tsx`: two options over `LayoutPicker`, each with an inline SVG — `GRID` two rows of small boxes, `SLIDER` one row running off the right edge with the last box clipped — drawn at one shared box size. Verify with a component test (`category-layout-picker.test.tsx`): both options render as radios, clicking one reports its key, and every card is free of percentage heights.
- [x] 5.3 In `features/ui/home-sections/home-sections-page.tsx`, pass a `settings` node for the `FEATURED_CATEGORIES` row containing the picker, bound to `section.variant ?? 'GRID'` and writing through the same per-section update path the enabled switch uses. Add a comment on the row explaining why this section's layout is chosen here while the hero's is not (design.md Decision 2). Verify by choosing Slider, saving, and confirming the `PATCH /settings` body carries `variant: 'SLIDER'` on that entry and nothing else changed; then that `GET /settings` shows the choice selected on reload. **Verified by the page-level test** (`section-layout-preserved.test.tsx`): the sent `homeConfig` equals the stored list with only that entry's `variant` changed, the payload carries only `homeConfig` and `newsletter`, and a stored `SLIDER` renders selected on load.
- [x] 5.4 Confirm the panel is offered while the section is disabled and that the page's unsaved-changes guard and failed-save behaviour apply to the layout: switch the section off, change the layout, navigate away → warned; make the server refuse the save (temporarily send an invalid value) → selection stays and the message shows. Verify each in a browser. **Two of three verified by the page-level test:** the panel opens and saves `SLIDER` while the section is off, and a refused save leaves Slider selected. **The navigate-away warning is by construction** — the layout is written through `draft.set`, and `useUnsavedChangesGuard(draft.isDirty)` is what the page already blocks on — and not browser-verified here.
- [x] 5.5 Extend `features/ui/home-sections/hero-variant-preserved.test.tsx` (rename to `section-layout-preserved.test.tsx`) so that a stored `SLIDER` on `FEATURED_CATEGORIES` survives an unrelated reorder and an enabled toggle, alongside the existing hero cases. Verify `npm run test --workspace admin -- src/features/ui/home-sections` passes.

## 6. Verification across the three apps

- [x] 6.1 Registries agree: the three tuples (`server` `FEATURED_CATEGORIES_VARIANTS`, `nextjs` `SECTION_LAYOUTS.FEATURED_CATEGORIES`, `admin` `FEATURED_CATEGORIES_LAYOUT_OPTIONS`) list `GRID` then `SLIDER`. Verify by inspection and by `npx tsx scripts/verify-section-variants.ts` on the server side. **Verified:** the storefront's `section-layouts.test.ts` pins its tuple to `["GRID", "SLIDER"]` exactly, the admin's options are `GRID` then `SLIDER`, and the server script passes for both sections.
- [x] 6.2 `GRID` is byte-identical: with the section on `GRID` (and with no layout stored at all), the storefront's Featured Categories HTML matches the pre-change capture from task 3.1. Verify by diff. **Verified both ways: 13,926 bytes, identical.**
- [ ] 6.3 Tile size parity: in a browser at 1440 content width, measure one tile in `GRID` and in `SLIDER`; widths and heights match. Repeat at a `sm` width. Verify by inspection (this cannot be tested in either suite — design.md Risks). **By construction:** `CategorySlider` states `slidesPerView` 3/4/7 at 640/1024 and `spaceBetween` 16 as named constants with a comment binding them to the grid's `grid-cols-3 gap-4 sm:grid-cols-4 lg:grid-cols-7`, and both layouts render the same `CategoryTile`. The measurement still needs a human. **Height parity fixed after a phone screenshot:** three tiles ended at three different lines because a slide stretches to the tallest but the tile inside it did not — the grid gets that stretch free from its row. `SwiperSlide` is now `!flex !h-auto [&>a]:flex-1`, so the tile fills its slide in both axes; done on the slide, not with `h-full` on the shared tile, so `GRID` stays byte-identical. Confirmed served and emitted.
- [ ] 6.4 Home Slider is unaffected by the shell extraction: open it, click each of the four hero layouts, and confirm the cards are one height, the click saves, and the slot grid below follows. Verify in a browser. **`variant-picker.test.tsx` passes unchanged** (8/8) over the extracted shell — click reports the layout, a save in flight leaves the other cards live, no percentage heights, one `viewBox`. The browser pass still needs a human.
- [x] 6.5 Full suites clean: `npm run lint` and `npx tsc --noEmit` in all three workspaces; `npm run test --workspace admin`; `npm run test --workspace nextjs`; the server verify script. Verify all pass. **Verified, with three pre-existing caveats stated rather than hidden:** tsc clean in all three; storefront 223/223; server script passes for both sections; admin 392 tests — two (`tax-rule-form-page`, `blog-form-page`, untouched by this change) hit the 5s default timeout under a three-suite parallel run and pass in isolation. Lint: every file this change touched is clean; the admin's one project-lint error is the pre-existing `use-order-alert.ts:64`; the storefront's `npm run lint` is bare `eslint` and its config deliberately un-ignores `.next/**`, so it reports ~430 errors in build output — `eslint src` is 0 errors, 1 pre-existing warning in `ProductSection.tsx`.
- [ ] 6.6 Deploy order stated and followed: server, then storefront, then admin (design.md — Migration Plan). Verify each is deployed before the next by checking `GET /settings/public` reports a resolved layout on the section before the storefront ships, and the storefront renders `SLIDER` before the admin ships. **Not done here — nothing has been committed or deployed from this session.** Locally the order was exercised in that sequence against the running stack (server 1.3 → storefront 3.5 → admin 5.3).
