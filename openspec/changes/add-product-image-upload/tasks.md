## 1. Validation and interface

- [x] 1.1 In `product.validation.ts`, add `imageSlots: z.array(z.object({ altText: z.string().max(200).optional(), sortOrder: z.number().int().optional(), isPrimary: z.boolean().optional() })).optional()` to `createProductZodSchema` (inherited by `updateProductZodSchema` via `.partial()`).
- [x] 1.2 In `product.interface.ts`, add the matching `imageSlots?: Array<{ altText?: string; sortOrder?: number; isPrimary?: boolean }>` field to `ICreateProductPayload`.

## 2. Route wiring

- [x] 2.1 In `product.route.ts`, add `multerUpload.array("images", 10)` immediately before `validateRequest(createProductZodSchema)` on `POST /` and before `validateRequest(updateProductZodSchema)` on `PATCH /:id` (design.md Decision 2) — matching where `multerUpload.single("image")` sits relative to `validateRequest` on `PATCH /auth/me`.

## 3. Controller: merge uploaded files into the payload

- [x] 3.1 In `product.controller.ts`, before calling `ProductService.createProduct`/`updateProduct`: if `req.files` (typed as `Express.Multer.File[]`) is non-empty, `Promise.all` an `uploadFileToCloudinary(file.buffer, file.originalname)` call per file (design.md Decision 4 — all uploads resolve, or the whole request fails, before any service/DB call).
- [x] 3.2 Zip the resolved upload results with `req.body.imageSlots` by index (`imageSlots[i]` describes `req.files[i]`, per design.md Decision 1) into `{ url: uploadResult.secure_url, altText: slot?.altText, sortOrder: slot?.sortOrder, isPrimary: slot?.isPrimary }` objects; append them to `req.body.images` (creating the array if the request had none). Do not mutate any existing URL-based entries already in `req.body.images`.
- [x] 3.3 Remove `imageSlots` from the payload passed to the service (it's already merged into `images`; the service's `IUpdateProductPayload`/`ICreateProductPayload` destructuring in `product.service.ts` doesn't need to change if `imageSlots` is simply not read there, but confirm it's not accidentally spread into the Prisma `data` object via `...rest`). Confirmed: `mergeUploadedImages` runs after `validateRequest` (so `imageSlots` is already the zod-parsed field) and `delete`s it from `req.body` before the service is called, so `product.service.ts`'s `{ variants, images, attributes, ...rest }` destructuring never sees it — no change needed there.

## 4. Error handling

- [x] 4.1 Confirm (or add, if missing) that `globalErrorHandler` produces a client-friendly `AppError`-shaped response for multer's own errors (e.g. exceeding the 10-file cap on `.array("images", 10)`), matching design.md's flagged risk. Was missing — `MulterError` fell into the generic `Error` branch (500, raw multer message). Added a dedicated branch: 400 status, friendly message for `LIMIT_FILE_COUNT`/`LIMIT_UNEXPECTED_FILE`, falling back to `err.message` for any other multer error code.

## 5. Verification

- [x] 5.1 Run the backend's typecheck/build/lint: `npx tsc --noEmit`, `npx eslint`, and `npm run build`.
- [x] 5.2 Manually exercise `POST /products` as `multipart/form-data` with a `data` field and 2+ image files against a running instance; confirm the created product's `images` includes one `ProductImage` per uploaded file plus any URL-based entries in `data.images`. Verified against the live Neon DB + the Cloudinary account then configured in `.env`: 201, 3 `images` returned (1 URL entry + 2 uploaded files with real `res.cloudinary.com` URLs), `imageSlots` metadata (`altText`/`isPrimary`/`sortOrder`) correctly attached to each uploaded entry.
- [x] 5.3 Manually exercise `PATCH /products/:id` mixing a kept existing image (by `id`), a new URL image, and a new uploaded file in one request; confirm all three are reflected correctly and any omitted existing image is removed (spec.md "mixing kept, URL, and uploaded images" scenario). Verified: kept image's `id` preserved with updated `altText`, new URL image created, new uploaded file created; the two images from 5.2 not re-listed in this request's payload were removed — matches `syncProductImages`'s existing reconcile behavior exactly.
- [x] 5.4 Confirm a plain `application/json` create/update request (no files) still works unchanged (spec.md "Plain JSON product create/update is unaffected" scenario). Verified: `PATCH` with `Content-Type: application/json` and no `images` field updated `description` only, left all 3 images untouched — multer/imageSlots merge path is a no-op with no files present.
- [x] 5.5 Confirm a request exceeding the 10-file cap returns a clean error, not an unhandled exception. Verified: 11 files → 400 `"Unexpected file field in upload"` (multer 2.x's `.array(field, max)` reports `LIMIT_UNEXPECTED_FILE` past the cap, not `LIMIT_FILE_COUNT` — the new `MULTER_ERROR_MESSAGES` map in `globalErrorHandler.ts` covers both codes). Confirmed `checkAuth` still runs before `multerUpload` in the route chain, so an unauthorized request never reaches file parsing.
- [x] 5.6 **New, added during verification**: the Cloudinary credentials in `.env` were found to belong to a different project; the user replaced them, and the new `cloud_name`/`api_key`/`api_secret` now authenticate successfully (`cloudinary.api.resources` succeeds). Re-verified 5.2 end-to-end against the corrected credentials via the exact `FormData` shape the admin frontend now builds (`buildProductForm` in `lib/api/products.ts`, see `add-product-image-upload-admin`): 201, both files uploaded to Cloudinary, `imageSlots` metadata correctly attached by position. Test product and its DB row deleted afterward; the two small test images this probe uploaded to Cloudinary were left in place rather than risk another bulk/prefix-based delete after the earlier incident this session (see `add-product-image-upload-admin` for that account) — harmless orphaned 1x1 PNGs, identifiable by original filename if ever cleaned up manually.
