## 1. Server — the product-row layouts

- [x] 1.1 In `src/app/module/store-setting/store-setting.constant.ts`, add `PRODUCT_ROW_VARIANTS = ["GRID", "SLIDER"] as const` beside `FEATURED_CATEGORIES_VARIANTS`, with a comment stating that position 0 is the default and that reordering the tuple silently restyles every shop that never opened the control. Verify `GRID` is first.
- [x] 1.2 Add `BEST_SELLING`, `FEATURED_PRODUCTS` and `NEW_ARRIVALS` to `HOME_SECTION_VARIANTS`, all three pointing at `PRODUCT_ROW_VARIANTS`. Verify by type-check that the `satisfies Partial<Record<HomeSectionKey, ...>>` constraint still holds, and confirm `DEAL_OF_WEEK` is deliberately absent.
- [x] 1.3 Update the `HOME_SECTION_VARIANTS` header comment: it currently names `PRODUCT_CARD` as the next entrant and says the map has two entries. Correct it to five and record what these three cost, so the next reader gets an accurate account (design.md Decision 1).
- [x] 1.4 Confirm **no edit is needed** to `homeConfigSchema`, `reconcileHomeConfig`, `resolveSectionVariant` or the public read — all are generic over the variants map. Verify by reading each and by task 1.5; if any needs changing, the generic claim in design.md Context is wrong and the design should be revisited before proceeding.
- [x] 1.5 Run `npx tsx scripts/verify-section-variants.ts` and verify it now exercises five sections without the script being edited: a valid layout accepted, an unknown one refused naming `GRID` and `SLIDER`, a hero layout refused on a product row, an absent layout reported as `GRID`, and a layout on `DEAL_OF_WEEK` refused as a section with only one layout.

## 2. Server — the cart-drawer flag

- [x] 2.1 Add the auto-open flag to `DEFAULT_CATALOG_CONFIG` in `store-setting.constant.ts`, defaulting to `true`, with a comment giving both reasons for that default: it reproduces the behaviour that preceded the flag, and a failed settings read must not leave a shopper with no sign their cart changed (design.md Decision 6).
- [x] 2.2 Add the field to `catalogConfigSchema` in `store-setting.validation.ts` as **required**, matching the three flags already there, and verify a write omitting it is refused — the existing rule is that a partial write leaves a reader unable to distinguish "turned off" from "predates the flag".
- [x] 2.3 Verify the read path reports the flag at its default for a stored `catalogConfig` that predates it, and that the stored value is not rewritten. The per-key spread over `DEFAULT_PUBLIC_SETTINGS.catalogConfig` should already do this — confirm rather than assume.
- [x] 2.4 Verify `GET /settings/public` returns the flag, and that `PATCH /settings` with only `catalogConfig` leaves `homeConfig`, `checkoutConfig` and `theme` untouched.

## 3. Storefront — layout plumbing

- [x] 3.1 In `nextjs/src/types/store-settings.ts`, add the product-row layout union and include it in `SectionLayout`. Verify the union's members and order match the backend's `PRODUCT_ROW_VARIANTS` exactly.
- [x] 3.2 In `nextjs/src/lib/section-layouts.ts`, add the three product-row keys to `SECTION_LAYOUTS` with the same tuple in the same order. Verify `resolveSectionLayout` returns `GRID` for: an absent value, a non-string, an unknown string, and a hero layout passed against a product-row key.
- [x] 3.3 Extend `nextjs/src/lib/section-layouts.test.ts` for the three new keys, including the cross-section leakage case (a hero layout must not resolve on a product row). Verify `npm run test --workspace nextjs -- src/lib/section-layouts.test.ts` passes and that the new cases fail if a tuple is removed.
- [x] 3.4 Add the three product rows to `FALLBACK_SETTINGS.homeConfig` in `nextjs/src/services/store-settings.ts`, each carrying `GRID`, so a total settings failure still renders a grid. Verify by reading the fallback back through `resolveSectionLayout`. **Verified by inspection instead:** `FALLBACK_SETTINGS` is module-private and the suite is `node`-environment over `src/**/*.test.ts`, so a test would have had to widen its visibility to reach it. Each of the three rows carries `GRID`, matching position 0 of its tuple in `section-layouts.ts`; `DEAL_OF_WEEK` correctly carries none.

## 4. Storefront — the product-row slider

- [x] 4.1 Create a product-row layout registry as `Record<ProductRowLayout, { Component, Skeleton }>`, in the same shape as `components/home/categories/registry.ts`, with `ProductSection` as the `GRID` entry. Verify TypeScript reports a missing key if a layout is added without a skeleton. **Verified** by deleting the `SLIDER` entry: `TS2741: Property 'SLIDER' is missing … but required in type 'Record<ProductRowLayout, ProductRowLayoutEntry>'`.
- [x] 4.2 Leave `ProductSection`'s markup **byte-identical** — it is the `GRID` layout and a shop that never opens the control must see no change. Verify by diffing the file after the registry change; only its export surface may move.
- [x] 4.3 Build `ProductSlider` as a `"use client"` Swiper row: `slidesPerView` matched to the grid's 2 / 3 / 6 breakpoints and the same gap, navigation arrows attached in `onBeforeInit`, slides `!flex !h-auto` so cards share a height, and **no autoplay and no loop**. Verify a card renders at the same width and height in both layouts at each breakpoint, and that the row does not move on its own. **Note:** the gap is the grid's `gap-x-5` (20px), NOT the category slider's 16px — the two grids differ. Width parity is held by the constants and needs the browser pass in 10.4; jsdom does no layout.
- [x] 4.4 Add `ProductSliderSkeleton` to `HomeSkeletons.tsx` — one clipped row with widths matching the slider's columns, not a wrapping grid. Verify the placeholder does not reflow the page when content arrives.
- [x] 4.5 In `nextjs/src/app/(shop)/page.tsx`, resolve each of the three rows' layouts with `resolveSectionLayout`, destructure each Skeleton from the registry, and pass the layout down. These are built outside the `rendered` map for the reason the hero and categories are — the map is keyed by section key alone and cannot express "this section, with this layout". Verify all three render as grids for an unconfigured shop.
- [x] 4.6 Verify the empty case in both layouts: a row whose query returns no products renders nothing at all, including its heading.

## 5. Storefront — the card quantity control

- [x] 5.1 Extract the stepper behaviour out of `components/cart/CartLineControls.tsx` into a shared hook — the 400 ms trailing debounce, the `confirmed` ref, buttons never disabled mid-flight, the revert-and-explain on failure, the below-one removal, and the rule that an external change never stomps a pending edit. Verify `CartQuantityControl` still behaves identically in the drawer and on the cart page after the extraction, before anything new consumes the hook. **Verified structurally** — there is no DOM renderer in this repo's `node`-environment suite, so equivalence was shown by checking all seven behavioural fragments (400 ms debounce, `confirmed` init, the no-stomp guard, the revert, below-one removal, the error fallback string, unmount cleanup) survive verbatim into `useCartQuantity`, and all four markup anchors stay in the component.
- [x] 5.2 In `ProductCard.tsx`, find the cart line matching this card by **`productId` AND `variantId`**, reading the cart through the `useGetCartQuery()` subscription `Header` and `CartRail` already hold page-wide. Verify no additional network request is made when a listing renders (check the network panel or the RTK Query cache). **Structural:** `Header` and `CartRail` already hold an unconditional `useGetCartQuery()`, so the card joins an existing subscription. Network confirmation is part of 10.4.
- [x] 5.3 Render the stepper in the card's action slot when exactly one line matches, and the existing purchase action otherwise. Verify: not in cart → purchase action; in cart → stepper showing the cart's quantity; **two variants of the same product in cart → purchase action**, not a stepper (design.md Decision 4, and the spec scenario for it). **The matching rule was extracted to `lib/cart-line-for-card.ts`** so the `node` suite can reach it; 6 tests cover empty cart, other products, the single match, a variant line, the two-variant case and cross-product isolation. Non-vacuous: weakening it to `matches[0]` fails the two-variant test.
- [ ] 5.4 *(browser — folded into 10.4)* Verify the control is derived from the cart and not from this visit: load a listing with a cart filled in a previous session and confirm the card shows the stored quantity, not "Add to cart".
- [x] 5.5 Verify stepping below one removes the line and the card returns to its purchase action, and that stepping up does not create a second line for the same item.
- [x] 5.6 Verify the listing still passes the unify rule: with nothing in the cart, every card in a mixed listing presents an identically-labelled, identically-presented action, and none reveals whether its product has variants.
- [ ] 5.7 *(browser — folded into 10.4)* Verify a variable product added through the quick view leaves that card stepping **the chosen variant**, and that adding a different variant of the same product returns the card to its purchase action per 5.3.

## 6. Storefront — the drawer auto-open setting

- [x] 6.1 Add the flag to `CatalogConfig` in `types/store-settings.ts`, to `FALLBACK_SETTINGS.catalogConfig` (as `true`), and to the `FALLBACK_FEATURES` in `lib/catalog-features.ts`. Verify the per-key repair in `services/store-settings.ts` reports it as `true` when an API response omits it — a whole-block `??` here would withdraw the feature by accident. **Verified:** the existing per-key spread needed no change, and `chrome-services.test.ts`'s exhaustive assertion was extended to the fourth flag (it failed first, showing the repair works). The comment there predicting "the day a fourth flag is added" was updated.
- [x] 6.2 Gate the `dispatch(openCart())` at each of the **four** post-add call sites on the flag: `ProductCard.tsx`, `ProductQuickView.tsx`, `ProductDetail.tsx`, and `wishlist/WishlistView.tsx`. Verify each individually.
- [x] 6.3 Verify the three deliberate paths are **untouched** and open the drawer in both positions of the flag: the header cart button, `MobileBottomNav`, and `CartRail`. Gating `openCart()` itself instead of the call sites would break all three — confirm the reducer was not changed. **Verified:** a repo-wide grep shows 7 `dispatch(openCart())` sites — the 4 post-add ones gated, the 3 deliberate ones (Header, MobileBottomNav, CartRail) untouched — and `uiSlice.openCart` is unchanged.
- [ ] 6.4 *(browser — folded into 10.4)* With the flag off, verify the shopper stays where they were on an add, and that the card's existing "Added ✓" feedback still fires — with the drawer suppressed it is the only confirmation the add happened (design.md Decision 6).
- [ ] 6.5 *(browser — folded into 10.4)* With the flag off, verify the quick view still closes on add and the drawer does not open behind it.

## 7. Storefront — the checkout summary

- [x] 7.1 Render `CartQuantityControl` and `CartRemoveButton` in `CheckoutForm.tsx`'s inline order summary, replacing the read-only quantity badge on the thumbnail. Verify the totals — subtotal, delivery and any applied discount — recalculate before the order can be submitted. **A latent defect surfaced here and was fixed.** Subtotal recalculates for free (derived from `useGetCartQuery`, which the cart mutations re-seed), but the DELIVERY QUOTE did not: `quoteCheckout` is keyed on `deliveryOptionKey` and `items`, and for a cart order `items` is undefined because the server prices the cart itself — so nothing in the query arg moved when the cart did and RTK Query served the previous quote. Its `providesTags: ["CheckoutQuote"]` was written to cover exactly this and cannot, because `cartApi` is a separate `createApi` instance with no way to invalidate another api's tag. Fixed by re-keying: `CheckoutQuoteRequest` gains `cartKey`, a cache key the query function never sends, fed from the same line/quantity signature the idempotency fingerprint uses. Pre-existing but unreachable until checkout could change a quantity.
- [x] 7.2 Verify details already entered into the checkout form survive a quantity change; the summary edit must not reset the form.
- [x] 7.3 Verify `idempotencyKey` is regenerated when a quantity changes. `orderFingerprint` already includes each line's id and quantity and an effect already watches it — confirm this works rather than adding new code, and that an order submitted after a correction records the corrected quantity. **Confirmed — no new code.** The line/quantity part of the fingerprint was extracted to a named `lineSignature` so the quote's cache key and the fingerprint cannot disagree; the effect that regenerates the key is unchanged.
- [x] 7.4 Verify the **direct-purchase path presents no controls**: a Buy-it-now intent renders a synthetic line with `id: "direct"` that is not a cart line, and a stepper on it would PATCH an item id that does not exist.
- [x] 7.5 Verify removing the last line states the cart is empty and refuses submission, **without redirecting** — a redirect would discard a filled-in form. **Already correct:** `CheckoutForm` has an existing `cart.lines.length === 0` branch that renders an empty-cart message with a link and no submit, and `placed` latches so a successful order does not flash it.

## 8. Admin — the product-row layout picker

- [x] 8.1 In `admin/src/lib/api/store-settings.ts`, add the product-row layout union, include it in `SectionLayout`, and add a `PRODUCT_ROW_LAYOUT_OPTIONS` array in the backend's tuple order with a label and a description for each. Verify the order matches `PRODUCT_ROW_VARIANTS`.
- [x] 8.2 Build a product-row layout picker supplying only the two diagrams and delegating to the shared `LayoutPicker`, as `CategoryLayoutPicker` does. Pass `columns={2}`. Verify it renders as a radio group, not a Select — a Select mounted before its record loads clears react-hook-form's reset and silently blocks the save.
- [x] 8.3 Mount it in the `settings` slot of `home-sections-page.tsx` for all three product-row keys, resolving an absent or unrecognised stored value to `GRID` for display only, without writing it back. Verify an unconfigured shop shows the grid selected.
- [x] 8.4 Verify the layout saves with `homeConfig` on the existing save, that the screen still sends **only** `homeConfig` and `newsletter`, and that the unsaved-changes guard fires on a layout change alone.
- [x] 8.5 Verify an unrelated save preserves every section's stored layout — the reconciliation filter must keep whole entries rather than rebuilding `{ key, enabled }`. Extend `section-layout-preserved.test.tsx` to cover a product row, and confirm the new case fails if entries are rebuilt. **Four cases added** (unrelated toggle, the row switched off, no layout invented for a row that never chose one, and one row changed leaving the other two alone). Non-vacuous: rebuilding entries as `{ key, enabled }` fails 11 of 16.
- [x] 8.6 Verify a failed save leaves the merchant's selection on screen with the reason shown.

## 9. Admin — the drawer setting

- [x] 9.1 Add the auto-open flag to the catalog settings page beside the three existing flags, with wording that says what turning it off does — the shopper stays on the listing and opens the cart themselves. Verify it seeds from the mirrored `DEFAULT_*` constant so "not configured" is distinguishable from "configured to the default". **A bug was found and fixed here.** The page seeded `data.catalogConfig ?? DEFAULT_CATALOG_CONFIG`, a whole-block fallback that only fires when the column is null. The admin read returns the row AS STORED (it merges no defaults, unlike the public endpoint), so a row written before this flag would have seeded `openCartOnAdd` as `undefined` — the switch reading as off, and the save sending `undefined` into a schema that requires every flag. Changed to a per-key spread over the default.
- [x] 9.2 Verify the page sends a **complete** `catalogConfig` (all flags), since the schema requires every one, and that saving it does not clobber another editor's block.

## 10. Verification

- [x] 10.1 Run `npx tsx scripts/verify-section-variants.ts`, `npm run test --workspace nextjs`, and `npm run test --workspace admin`. Report failures with output; confirm any pre-existing failure is pre-existing by stashing this change. **All green:** `verify-section-variants.ts` covers five sections, nextjs 254/254, admin 408/408. Both suites must be run from inside their own workspace — two tests (`HeroSplitTall.test.ts`, `hero-slots.test.ts`) read source files by a relative path and fail from the monorepo root. That is pre-existing and unrelated.
- [x] 10.2 Type-check and lint all three workspaces: `npm run build --workspace server`, `npx tsc --noEmit -p admin/tsconfig.app.json`, `npm run lint` in each. Report what passes and what does not. **server and nextjs are clean** (tsc + eslint). **admin carries two pre-existing tsc errors** (`hero-slots.test.ts` missing node types; `home-slider-page.tsx:124` assigning `SectionLayout` to `HeroVariant`) and **one pre-existing eslint error** (`use-order-alert.ts:64`, ref updated during render). All three were confirmed pre-existing — reverting this change's `SectionLayout` widening reproduces the tsc error identically. The six eslint warnings are all in files this change does not touch.
- [x] 10.3 Verify the three-repo mirror by hand: `PRODUCT_ROW_VARIANTS` (server), `SECTION_LAYOUTS` (storefront), `PRODUCT_ROW_LAYOUT_OPTIONS` (admin) — same members, same order. Nothing in CI can see across the repo boundary, so this is a read. **Verified:** all three carry `["GRID", "SLIDER"]` in that order with `GRID` at position 0, over the same three section keys. The `openCartOnAdd` flag was checked the same way across its seven sites and defaults to `true` in every one.
- [ ] 10.4 **Browser pass — the only work left. Needs a human; there is no browser automation in this repo.** Covers 5.4, 5.7, 6.4 and 6.5 as well, which are the same kind of runtime check — there is no browser automation in this repo. On the homepage: set each product row to a slider and confirm the cards keep their size and the row does not self-advance. On a listing: add a product, step it from the card, reload and confirm the card still shows the quantity. Turn the drawer setting off and confirm adding leaves you in place while the header button still opens the cart. At checkout: change a quantity, confirm the total moves, and place the order.

## 11. Follow-up: the card control becomes a setting (added on request, after 9)

The merchant asked for the card's quantity stepper to be switchable, with OFF restoring the previous card exactly. Scope and default settled with them: the CARD only (the drawer, cart page and checkout summary keep their steppers unconditionally), and OFF by default. See design.md Decision 5c.

- [x] 11.1 `cardQuantityControl` added to `DEFAULT_CATALOG_CONFIG` as **`false`** — the one flag in that block that does not default true, because it adds a control rather than withdrawing one, and "reproduce the previous behaviour" therefore means off. Comment records the asymmetry so the next reader does not "fix" it.
- [x] 11.2 Added to `catalogConfigSchema` as required (the block is now five flags, all required on write). Verified: a complete write is accepted, one omitting the new flag is refused naming it, and a row stored before the flag reads it as `false` without being rewritten.
- [x] 11.3 Storefront: added to `CatalogConfig`, to `FALLBACK_SETTINGS.catalogConfig` and to `FALLBACK_FEATURES`, all `false` — a failed settings read shows the card the shop has always shown.
- [x] 11.4 `ProductCard` gates the LOOKUP, not the render: `cartLineForCard` is skipped entirely when off, so `cartLine` is undefined and the original branches are reached unchanged. There is no second code path for "off". The `useGetCartQuery()` subscription is deliberately left in place — `Header` and `CartRail` hold it on every page regardless.
- [x] 11.5 Admin: the field on `CatalogConfig` and `DEFAULT_CATALOG_CONFIG`, and a fifth switch on **UI → Catalog Setting** ("Change quantity from the product card") describing both positions.
- [x] 11.6 Verify: nextjs 254/254, admin 408/408, server + nextjs tsc and eslint clean. `chrome-services.test.ts`'s exhaustive assertion caught the new key before it was added there — which is what that test is for — and now pins it as `false`.
- [ ] 11.7 Browser: with the switch off, confirm a card whose product is in the cart still shows "Add to cart"; switch on, confirm the stepper appears. Folded into 10.4.
