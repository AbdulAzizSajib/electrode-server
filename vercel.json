{
  "version": 2,
  "buildCommand": "npm run build",
  "outputDirectory": ".",
  "installCommand": "npm install --production=false --include=dev",
  "functions": {
    "api/index.js": {
      "maxDuration": 30,
      "includeFiles": "src/app/templates/**"
    }
  },
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/api"
    }
  ]
}
